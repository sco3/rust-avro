[package]
name = "rust-avro"
version = "0.1.0"
edition = "2024"

[dependencies]
apache-avro = "0.21.0"
clap = { version = "4.6.1", features = ["derive"] }
serde_json = "1.0.150"
tracing = "0.1.44"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
